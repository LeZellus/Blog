{# read and display just one flash message type #}
{% for message in app.flashes('notice') %}
    <div class="flashes bg-blue-50 text-blue-400 border-blue-400 flex">
        <p class="text-blue-400 font-light"><span class="text-blue-500 uppercase font-semibold">Notification : </span>{{ message }}</p>
        <i class="notif-close ph-x-square text-3xl cursor-pointer text-blue-500"></i>
    </div>
{% endfor %}

{# read and display just one flash message type #}
{% for message in app.flashes('success') %}
    <div class="flashes bg-green-50 text-green-400 border-green-400 flex">
        <p class="text-green-400 font-light"><span class="text-green-500 uppercase font-semibold">Succès : </span>{{ message }}</p>
        <i class="notif-close ph-x-square text-3xl cursor-pointer text-green-500"></i>
    </div>
{% endfor %}

{# read and display just one flash message type #}
{% for message in app.flashes('error') %}
    <div class="flashes bg-red-50 text-red-400 border-red-400 flex">
        <p class="text-red-400 font-light"><span class="text-red-500 uppercase font-semibold">Erreur : </span>{{ message }}</p>
        <i class="notif-close ph-x-square text-3xl cursor-pointer text-red-500"></i>
    </div>
{% endfor %}

{# read and display just one flash message type #}
{% for message in app.flashes('warning') %}
    <div class="flashes bg-yellow-50 text-yellow-400 border-yellow-400 flex">
        <p class="text-yellow-400 font-light"><span class="text-yellow-500 uppercase font-semibold">Attention : </span>{{ message }}</p>
        <i class="notif-close ph-x-square text-3xl cursor-pointer text-yellow-500"></i>
    </div>
{% endfor %}